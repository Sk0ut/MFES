class TrapTile is subclass of Tile
values
	public TYPE : TileType = "TrapTile";
operations
	public TrapTile: Owner ==> TrapTile
				TrapTile(ow) == Tile(TYPE, ow)
	post type = TYPE and owner = ow;
end TrapTile