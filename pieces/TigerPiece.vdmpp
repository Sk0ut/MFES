class TigerPiece is subclass of Piece
values
	public TYPE : PieceType = "TigerPiece";
	public RANK : nat1 = 6;
operations
	public TigerPiece: Owner ==> TigerPiece
				TigerPiece(ow) == Piece(TYPE, ow, RANK)
	post type = TYPE and owner = ow and rank = RANK;
end TigerPiece