class DogPiece is subclass of Piece
values
	public TYPE : PieceType = "DogPiece";
	public RANK : nat1 = 3;
operations
	public DogPiece: Owner ==> DogPiece
				DogPiece(ow) == Piece(TYPE, ow, RANK)
	post type = TYPE and owner = ow and rank = RANK;
end DogPiece